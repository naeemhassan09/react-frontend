export enum PRODUCT_SETTINGS {
  PER_PAGE = 20
}

export enum TAG_SETTINGS {
  PER_PAGE = 20
}

export enum ZONE_SETTINGS {
  PER_PAGE = 10,
  MAX =1000
}

export enum ED_REPORTING_SETTINGS {
  PER_PAGE = 10
}

export enum MOV_SETTINGS {
  PER_PAGE = 10,
  MAX = 1000,
  DEFAULT_INDEX = 1
}

export enum SKU_SETTINGS {
  PER_PAGE = 8,
  MAX =1000
}

export const enum TABS_VARIANT {
  DEFAULT = 'default',
  CTA = 'cta'
}

export const enum FORM_MODE {
  EDIT = 'edit',
  ONBOARDING = 'onboarding'
}

export enum CATEGORY_SETTINGS {
  PER_PAGE = 20,
  DEFAULT_INDEX = 1,
  MAX = 500
}

export enum ENTITY_STATUS {
  ENABLED = 0,
  DISABLED = 1
}