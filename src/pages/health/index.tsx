import React, { FC } from 'react';
import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

export const Health: FC = () => (
  <Box>
    <Typography>Product Portal is up and running</Typography>
  </Box>
);
